# Configurations

This directory is reserved for pipeline configuration files.

Planned config types:

- Baseline run configuration
- Matching-upgrade configuration
- Sample restriction definitions
- Travel-time definition variants
- QSM-ready export configuration

Current status:

- No configuration files have been created yet.
- Pipeline parameters are still embedded in notebooks.
